#include "main.h"
/**
 * swap_int - interchang the values of a and b
 * @a: first number
 * @b: second number
 *
 * Return:
 */
void swap_int(int *a, int *b)
{
	int temp;
	
	*a = temp;
	*a = *b;
	temp = *b;
}
