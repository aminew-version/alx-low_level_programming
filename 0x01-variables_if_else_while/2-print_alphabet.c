#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - 2. I sometimes suffer from insomnia.
 * Return: 0
 */
int main(void)
{
	int ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
		ch++;
	}
	return (0);
}
