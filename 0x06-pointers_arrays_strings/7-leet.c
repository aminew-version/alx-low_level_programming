#include "main.h"
/**
 * leet - encodes string in to 1337
 * @s: aray of string
 * Return: 0
 */
char *leet(char *s)
{
	int i, j;
	char special[20] = {'a', 'A', 'e', 'E', 'o', 'O', 't', 'T', 'l', 'L'};

	for (i = 0; s[i] != '\0'; i++)
	{
		if 
	}	
}
